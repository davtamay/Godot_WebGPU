# WebGPU Patch Stack - Manifest and Rules

## Current stack

| # | Subject | Purpose | Shared lines | Upstream PR |
|---|---------|---------|--------------|-------------|
| 00 | misc: Add WebGPU patch-stack tooling, CI baseline, and docs | Workflow bootstrap; zero engine changes | 0 | n/a (fork-only) |
| 01 | [shared] web: Add opt-in use_rendering_device SCons option | Defines RD_ENABLED on web (default off, requires threads=yes); RD stack becomes linkable, no driver | ~11 (platform/web/detect.py) | not yet |
| 02 | [shared] drivers: Add stubbed WebGPU driver scaffold | RenderingContextDriverWebGPU + RenderingDeviceDriverWebGPU + shader-container-format stubs under drivers/webgpu/, compiled on web when use_rendering_device=yes; unreachable at runtime | 2 (drivers/SCsub) | not yet |
| 03 | [shared] web: Add async WebGPU device pre-init to the loader | experimentalWebGPU config (default off): loader requests adapter/device before start, stashes Module.preinitializedWebGPUDevice (consumed by our own glue in later patches via Module.WebGPU.importJsDevice), warns + falls back to WebGL otherwise; engine still always boots GL until 05 | ~28 engine.js, ~11 config.js, ~12 features.js | not yet |
| 04 | [shared] web: Add webgpu option registering the WebGPU driver | webgpu=yes links the emdawnwebgpu port (Dawn-pinned remote port, needs network on cold builds), defines WEBGPU_ENABLED, implies use_rendering_device; "webgpu" advertised by DisplayServerWeb, boot probes the context and falls back to WebGL 2 (context still reports unavailable until 05) | ~12 detect.py, ~20 display_server_web.cpp | not yet |
| 05 | [shared] webgpu: Implement swap chain presentation with a probe clear | Real context (device import, canvas surfaces) + driver command/swap-chain/present paths; godot_webgpu_probe() presents a cleared frame on a hidden canvas (canvases are locked to their first context type); engine still renders WebGL until textures/shaders land. drivers/webgpu now requires webgpu=yes (not just use_rendering_device) | ~4 SCsub, ~12 display_server_web.cpp | not yet |
| 06 | webgpu: Implement the resource layer with staging uploads | Buffers (upload maps emulated via malloc shadow + wgpuQueueWriteBuffer; downloads deferred), textures + format table (uncompressed/depth/BC; ETC2/ASTC deferred), samplers (border colors degrade to clamp), copy commands, real device limits, benign timestamp/pipeline-cache stubs; probe now uploads a pattern through staging and copies it over the presented frame | 0 | not yet |
| 07 | [shared] webgpu: Bake shaders to WGSL through Tint at export time | Real shader container (SPIR-V pre-transform: 1.3 downgrade + entry-point interface strip + push-constants -> read-only SSBO at group 0 binding 510 w/ NonWritable members; external tint via GODOT_TINT_PATH, zstd WGSL); driver shader_create_from_container (modules + bind group layouts w/ combined-sampler remap + pipeline layout); baker platform plugin registered unconditionally (first cross-platform baker; metal/d3d12 are host-gated); web export shader_baker/enabled option; coverage 90/205 mobile-renderer variants, gaps documented in webgpu-testing.md | ~8 editor_node.cpp, ~5 export_plugin.cpp, ~4 baker SCsub, ~4 drivers/SCsub | not yet |
| 08 | webgpu: Implement the pipeline layer with a probe triangle draw | Render/compute pipelines (full state mapping), vertex formats, uniform sets -> bind groups (combined-sampler pair remap), push constants via a dynamic-offset ring buffer (bind groups defer to draw time: the offset arrives after uniform sets bind), general render passes/framebuffers, lazy compute passes, barrier no-ops (api_trait_get: WebGPU tracks hazards); probe draws a push-constant-colored triangle through shader_create_from_container + render_pipeline_create | 0 | not yet |
| 09 | webgpu: Add SPIR-V compatibility passes to the shader transform | Dead-binding elimination (glslang keeps declared-but-unused resources in every stage; Tint rejects texture/sampler array declarations even when unreferenced -- 80 of the 115 bake failures were the mobile VERTEX stage tripping on the unused lightmap array), completing the 1.3 downgrade (SignExtend/ZeroExtend image-operand strip, scalar-condition OpSelect splat), infinite-constant clamp, isInf/isNan rewrite as integer bit tests, PointSize store strip (points render 1px on web), OpMemoryBarrier -> control-barrier conversion; bake coverage ~96 -> 187 containers with zero Tint failures | 0 | not yet |
| 10 | webgpu: Fan arrayed uniforms out to one binding per element | The mobile renderer builds uniform sets with all 16 lightmap ids whether or not lightmaps exist, and WGSL has no binding arrays: bind group layouts + bind groups expand arrayed texture/sampler/image uniforms to consecutive bindings in a reserved range (ARRAY_BINDING_BASE 512 + binding*16 + element; Godot's dense binding numbering makes the sized-binding-arrays proposal's binding+i convention collide with neighbors). Modules that dead-eliminated the declaration still work: WebGPU allows layouts to declare bindings the shader does not use. Probe set 0 gains an unreferenced 4-texture array the harness hard-gates via a marker; the SPIR-V switch-codegen for genuinely dynamic indexing stays deferred until lightmap variants are exercised | 0 | not yet |
| 11 | webgpu: Support dynamic buffers with per-frame slices | The mobile renderer's render-pass uniform set uses UNIFORM_BUFFER_DYNAMIC + STORAGE_BUFFER_DYNAMIC unconditionally, so boot needs them: BUFFER_USAGE_DYNAMIC_PERSISTENT buffers allocate one alignment-padded slice per frame in flight (persistently mapped via the CPU shadow, flushed with wgpuQueueWriteBuffer), layouts mark the entries hasDynamicOffset, and binds decode the Vulkan-convention packed frame indices into offsets ordered before the push-constant ring's (binding order). Probe: the triangle's color is now multiplied by a tint read from the ADVANCED slice of a dynamic uniform buffer - selecting the wrong slice blacks the triangle out and fails the strict pixel check | 0 | not yet |

| 12 | webgpu: Bring up scene rendering in the browser | The umbrella bring-up patch: real texture-format capability reporting, staging-shadow sync at copy-encode time (RD keeps its staging ring persistently mapped and never unmaps - GPU buffers were zeros), 256-aligned row repacks, swizzle-free view handling with srgb siblings declared at creation, sampler validity fixes (no aniso on non-linear filters), storage-usage degradation for unsupported formats, main-thread proxying of driver object creation (emdawnwebgpu JS tables are per-thread), zero-initialized capability BitFields (default-constructed BitField reads stack garbage) | 0 | not yet |
| 13 | [shared] web: Boot the RenderingDevice backend in DisplayServerWeb | Native-pattern RD boot (context initialize -> window surface -> RenderingDevice -> screen_create -> RendererCompositorRD::make_current) with GL fallback when the WebGPU device is unavailable; over the line guideline, flagged in the commit | ~50 display_server_web.cpp/.h | not yet |
| 14 | [shared] web: Enable WebGPU in the exported loader | Exports with driver.web=webgpu emit experimentalWebGPU into the config (the loader pre-init never ran in exports before); requestDevice passes adapter-derived requiredLimits and gates optional features (float32-filterable) on the adapter | ~15 engine.js, ~5 export_plugin.cpp | not yet |
| 15 | [shared] editor: Bake all shader variants for WebGPU exports | The baker skipped variants the HOST considered disabled (host-vs-target divergence); bake everything the target can use instead. Baked-cache misses on web are fatal (no runtime compile), so coverage is correctness | ~30 shader_baker_export_plugin.cpp, ~10 shader_rd.cpp | not yet |
| 16 | webgpu: Cover instanced storage-buffer draws in the probe | Probe stage: two quad instances read per-instance data from a storage buffer through an index buffer - the exact canvas-item path that exposed the staging-sync bug - with strict pixel checks | 0 | not yet |
| 17 | webgpu: Emulate texture swizzles for font atlases | WebGPU has no view swizzle: LA8/L8/A8-class textures promote to RGBA8 at creation and expand texels CPU-side at upload (RG->RRRG, R->RRR1, R->000R); font glyphs rendered as white boxes before this | 0 | not yet |
| 18 | webgpu: Make debug label hooks silent no-ops | command_begin/end_label, breadcrumbs, and set_object_name were ERR_FAIL spam per object per frame; they are optional diagnostics | 0 | not yet |
| 19 | webgpu: Exclude untranslatable shader variants from bakes | Tint translation failure becomes a clean verbose exclusion instead of a per-variant error cascade; the runtime treats the absence as a cache hole | 0 | not yet |
| 20 | [shared] webgpu: Add SPIR-V preprocessing passes for Tint | drivers/webgpu/spirv_preprocess (adapted from dwalter/godotwebgpu, MIT): flatten_binding_arrays (WGSL has no handle arrays), lower_view_index_to_zero, and negate_position_y (Godot bakes the GL->Vulkan Y flip into projections; WebGPU NDC is Y-up, so every RT rendered mirrored - shadow lookups sampled the mirror and culling was inverted) wired before Tint; compiled editor-side for the baker | ~4 baker SCsub | not yet |
| 21 | webgpu: Preprocess SPIR-V modules before Tint translation | lower_spec_constant_ops_to_runtime: OpSpecConstantOp expression chains become regular instructions cloned per using function while plain OpSpecConstant stays a live WGSL override - preserving runtime specialization (the freeze fallback evaluated tonemap packed flags as 0 -> division by zero -> white screen) | 0 | not yet |
| 22 | webgpu: Sanitize Tint WGSL output for browsers | Reserved-word renames, chromium_disable_uniformity_analysis -> the standards-track diagnostic(off, derivative_uniformity) (stock browsers reject the Chromium enable without flags), atomic-taint text recovery for a Tint reader bug (one atomicAdd wrapped every member load in atomicLoad), round-tripped through Tint for revalidation | 0 | not yet |
| 23 | webgpu: Support the 3D renderer feature set in the driver | MSAA resolve attachments (Vulkan-style resolve references -> WGPURenderPassColorAttachment.resolveTarget), fragment-output write-mask zeroing, per-layout lazy bind groups (uniform sets are shared across variants; Dawn matches layouts structurally), comparison-sampler and depth-texture typing from WGSL scans with NonFiltering substitutes, per-module override filtering, layout-absent binding skip guards | 0 | not yet |
| 24 | webgpu: Allow renderer effect files as shared edits | check-stack rule extension for the renderer_rd effect/storage files the 3D bring-up needed | ~5 check-stack.sh | not yet |
| 25 | [shared] webgpu: Render the mobile pipeline without subpasses | WebGPU has no subpasses: WEBGPU_ENABLED gates force the mobile renderer's separate-pass path (using_subpass_post_process=false, FB_CONFIG degradation, subpass tonemap variants disabled, warmup pipeline formats single-pass) | ~20 across render_forward_mobile.cpp, tone_mapper.cpp | not yet |
| 26 | [shared] webgpu: Tolerate missing variants in baked shader caches | A single null variant no longer voids a whole cached shader version: platform-inexpressible variants are holes; version_get_shader returns null for them and call sites skip (copy_effects/rt_sdf loops hardened) | ~15 shader_rd.cpp + call sites | not yet |
| 27 | [shared] webgpu: Give the unused emission slot a distinct buffer | particles_storage bound the same placeholder buffer at two writable storage bindings (Vulkan-legal, WebGPU-forbidden aliasing); a distinct scratch buffer under WEBGPU_ENABLED keeps flag-off builds byte-identical | ~8 particles_storage.cpp/.h | not yet |
| 28 | [shared] webgpu: Skip pipelines for unavailable shader variants | Effect constructors blindly wrapped null variant shaders in pipelines (compute warmup error flood); skip guards consistent with existing is_variant_enabled patterns | ~10 across effects | not yet |
| 29 | [shared] webgpu: Drop unsupported storage usage from the VRS texture | The default VRS texture is created R8Uint+STORAGE unconditionally; ask texture_is_format_supported_for_usage first (it is only ever sampled) | ~6 texture_storage.cpp | not yet |
| 30 | [shared] webgpu: Bake raster octmap shaders for storage-limited targets | The editor bakes with the HOST GPU's rgb10a2-storage answer, but the web runtime takes the raster octmap path (no rgb10a2 storage in WebGPU) whose ShaderRDs were never instantiated at bake time - sky radiance was unbaked. An upstream-real gap (Android devices without rgb10a2 storage hit the same miss) | ~25 shader_baker_export_plugin.cpp | not yet |
| 31 | [shared] webgpu: Skip baked shader groups the target cannot use | Bake-side veto hook (ShaderBakerExportPluginPlatform::skips_variant): the WebGPU plugin skips multiview variants (WGSL cannot express ViewIndex; stereo will be one pass per view) and FP16 variants (driver reports SUPPORTS_HALF_FLOAT=false); they shipped as dead bytes -- the runtime never requests either. Skipped variants become tolerated cache holes (patch 26); bakes also get faster (no glslang/Tint for skipped variants) | ~10 shader_baker_export_plugin.cpp/.h | not yet |

| 32 | [shared] webgpu: Report unbaked shader versions with an actionable error | The web runtime cannot compile shaders, so a version missing from the baked cache produced a 3-errors-per-variant cascade; _compile_version_start now reports one actionable error naming the shader and the usual cause (materials constructed in scripts are invisible to the export-time baker; save them as .tres) and leaves the version invalid, which version_get_shader already handles | ~11 shader_rd.cpp | not yet |

| 33 | [shared] web: Choose the boot driver by the project's WebXR needs | New web-export option webxr/uses_webxr (default off) emits requiresWebXR into the exported config; the loader then keeps the WebGL driver on browsers that support WebXR but cannot render immersive sessions through WebGPU (a canvas locks to its first context type, so the choice is boot-time only). Detection is capability-based: Engine.isWebXRWebGPUAvailable() (XRGPUBinding) plus an engine-support constant that stays false until the WebXR-WebGPU rendering path lands. One export renders WebGPU on flat browsers and enters XR through the intact WebGL path elsewhere | ~10 export_plugin.cpp, ~13 engine.js, ~12 config.js, ~14 features.js | not yet |

| 34 | [shared] webxr: Bind WebXR sessions to the WebGPU renderer | When the WebGPU driver booted (signal: the pre-initialized device on Module), the WebXR glue creates an XRGPUBinding instead of the XRWebGLBinding, appends the mandatory 'webgpu' session feature (without it the browser mints a WebGL-based session the binding refuses), and creates the projection layer in the binding's DEFAULT shape (explicit textureType produced broken sub-images on Quest Browser's experimental impl). Layer textures cross to C++ as WGPUTexture handles (emdawnwebgpu importJsTexture, cached in a JS map) and the interface wraps them with RD texture_create_from_extension for the renderer-agnostic render-target override -- the OpenXR/Vulkan idiom. The multiview session requirement is skipped on this path (WGSL has none; GLES3::Config does not even exist here). Two presentation fixes ride along: the canvas returns null from getCurrentTexture() while an immersive session owns the compositor (guard makes it throw into the port's error path; the driver skips the frame silently). Adds modules/webxr to the shared allowlist. QUEST-3-VERIFIED: session starts, layer sub-image = 1680x1760 2-layer rgba8unorm array wrapped cleanly; rendering then stops at the renderer's multiview requirement = exactly the next patch's scope. Loader keeps steering XR exports to WebGL until stereo lands (ENGINE_WEBXR_WEBGPU_SUPPORTED stays false) | ~55 library_godot_webxr.js, ~60 webxr_interface_js.cpp, ~1 godot_webxr.h, ~30 webxr.externs.js, ~8 engine.js | not yet |

| 35 | [shared] xr: Draw XR viewports one pass per view without multiview | WGSL has no multiview, so stereo on the WebGPU backend renders the viewport once per view: XRInterface gains get_draw_pass_count()/set_current_draw_pass() (defaults keep every other interface and platform on the existing single-pass path) and the viewport's XR block loops them; the WebXR interface reports ONE view to the renderer, remaps the active pass onto view 0's transforms/projection (the mono camera path needs no changes), and hands each pass its view's layer slice via texture_create_shared_from_slice. Flips ENGINE_WEBXR_WEBGPU_SUPPORTED: XR-flagged exports now genuinely boot WebGPU on XRGPUBinding browsers | ~8 xr_interface.h, ~12 renderer_viewport.cpp, ~4 engine.js, rest in modules/webxr | not yet |

| 36 | [shared] webxr: Choose the XR session binding by the booted renderer | The glue picked XRGPUBinding whenever a pre-initialized WebGPU device existed on Module, but the XR-adaptive loader can prepare that device while a gl_compatibility project boots GLES3 - the session then fed WGPUTexture handles into the GL renderer and rendered nothing. godot_webxr_initialize now takes the booted renderer (RenderingDevice singleton presence) and only requests the 'webgpu' feature / attaches XRGPUBinding when renderer and bridge agree | ~12 library_godot_webxr.js, ~5 webxr_interface_js.cpp, ~1 godot_webxr.h | not yet |

| 37 | [shared] webxr: Expose real-world depth sensing to scripts | WebGPU sessions request gpu-optimized depth sensing when the feature is asked for; get_system_info() surfaces the depth map as an RD texture (webxr_depth_texture_rd + rawValueToMeters + size) plus a webxr_depth_status reason string so occlusion consumers explain fallbacks. Probed only on script demand, never per frame. Quest Browser 148's XRGPUBinding lacks getDepthInformation() (status: unsupported_by_browser) - the suite falls back to static room-mesh occlusion until a browser update ships it. KNOWN GAP: sampling the scene depth buffer (hint_depth_texture) is not yet bindable on the WebGPU driver (depth-aspect views + non-filtering sample type needed) - prerequisite for live-sensor occlusion there | ~48 library_godot_webxr.js, ~60 webxr_interface_js.cpp/.h, ~1 godot_webxr.h | not yet |

| 38 | webgpu: Reflect the probe's fragment output mask | The probe containers never set fragment_output_mask, so the write-mask-zeroing rule (a bring-up patch) made every probe draw rasterize invisibly - markers/validation green, strict pixels silently dead. Declaring color target 0 restores the strict probe as a regression gate | 0 | not yet |

| 39 | webgpu: Remove unused SPIR-V preprocessing passes | Nine never-wired passes from the dwalter integration (~1,700 unreachable lines incl. a duplicate of the container's inline push-constant rewrite) removed; live chain unchanged: flatten_binding_arrays, lower_view_index_to_zero, lower_spec_constant_ops_to_runtime, negate_position_y, freeze_spec_constant_ops | 0 | not yet |

| 40 | webgpu: Keep the per-draw bind path allocation-free | Stereo-frame hot-path fixes: per-draw dynamic-offset heap allocation -> fixed stack array; single dirty flag rebinding ALL sets per push-constant move -> per-set dirty bits + push-constant flag (sets without the ring entry stop rebinding per draw); per-pass color-attachment vector -> stack storage. Verified: strict probe pixel-exact + full-scene boot 0 errors | 0 | not yet |

| 41 | [shared] webxr: Fetch per-pass XR state in one thread crossing | Every godot_webxr_* call is a synchronous worker-to-main crossing; the RD pre-draw made two per pass (color, depth), each recomputing getViewSubImage. One merged call now returns [layer_generation, color, depth]; the sub-image is computed once per animation frame (helps the GL path too); and a layer-generation counter evicts the interface's cached RD wrappers when the projection layer is recreated (resize/view-count change) instead of leaking them for the session. Quest-verified: VR+AR sessions render and track as before | ~32 library_godot_webxr.js, ~45 webxr_interface_js.cpp/.h, ~1 godot_webxr.h | not yet |

| 42 | misc: Add a scheduled upstream auto-rebase workflow | Weekly (and on-demand) GitHub Action fetches upstream master, replays the stack, and only on a fully clean replay + check-stack pass pushes a dated auto-rebase/ candidate branch (plus a backup tag); the baseline workflow builds candidates automatically. webgpu-stack is never pushed by automation - promotion stays human because CI cannot run the tint bakes, the strict real-GPU pixel probe, or in-headset checks. A conflicted rebase fails loudly naming the patch | 0 (fork-only files) | n/a (fork-only) |

| 43 | misc: Harden the upstream sync for unattended operation | Self-keepalive (GitHub disables schedules after 60 idle days), concurrency guard, failure/candidate-ready GitHub issues (label upstream-sync - conflicts land in email instead of a silent Actions tab), Emscripten-pin drift check against upstream web_builds.yml, pruning of old candidates/backup tags (newest 3), and promote-candidate.sh encoding the human promotion ritual (dirty-tree/branch/stale-candidate refusals, snapshot, reset, printed local-gate checklist; never pushes) | 0 (fork-only files) | n/a (fork-only) |

| 44 | misc: Build tint in CI and publish it as a release asset | On-demand workflow builds the SPIR-V->WGSL translator from the Dawn tag the emdawnwebgpu port pins (Linux + Windows, tint command-line tools only, backends off), smoke-tests it, and uploads permanent release assets (tag tint-<ref>). Removes the hand-built-local-tint bus factor and is the prerequisite for a future CI bake-coverage leg; re-run when the Dawn pin moves | 0 (fork-only files) | n/a (fork-only) |

| 47 | webgpu: Auto-locate the tint binary beside the editor | GODOT_TINT_PATH still wins, but a tint executable next to the editor binary is picked up automatically - drop-in setup for fork users, no environment variable; the missing-tint error now names both options and the release assets. Verified both ways (beside-editor bake clean; absence = one actionable error) | 0 | not yet |

| 48 | [shared] webxr: Fetch all frame matrices in one thread crossing | The head transform and every view transform/projection each cost a synchronous worker-to-main crossing per call (~5 per stereo frame); process() now fetches them all in one call and the per-view getters read the cache (old calls kept as fallback for queries before the first frame). Quest-verified: tracking and eye alignment unchanged | ~27 library_godot_webxr.js, ~20 webxr_interface_js.cpp/.h, ~1 godot_webxr.h | not yet |

| 49 | [shared] webxr: Render GL stereo one pass per view without multiview | Android XR browsers ship WebGL without OVR_multiview2/OCULUS_multiview, and Godot GL stereo hard-required it (shader compile crash on Galaxy XR). Detected at layer creation: without it the projection layer is a shared side-by-side texture and the patch-35 draw-pass machinery renders each eye into its own render target, then blits it (Y-flipped; layer textures are bottom-left origin) into the eye viewport - the Unity-WebXR-export approach. Sub-images are only queried once the layer joins the ACTIVE render state (strict per-spec on Android XR; a layer set via updateRenderState only activates next frame). Quest keeps its multiview fast path. Galaxy-XR-verified: stereo, tracking, interaction | modules/webxr | not yet |

| 50 | [shared] webxr: Keep hand trackers honest across input-source churn | THE long-standing frozen-hands fix, diagnosed with raw-boundary telemetry on Quest 3 and Galaxy XR. Three compounding defects: (1) inputsourceschange processed additions before removals, so a source replaced within one event (browsers re-add hands in different modes constantly) had its stale removal wipe the slot its replacement just claimed - the hand went permanently silent; (2) removeInputSource cleared slots by index without checking identity; (3) when a hand input source vanished, its XRHandTracker kept the last joints flagged as live tracking data forever - consumers rendered a frozen hand. Hands now vanish cleanly on tracking loss and return automatically on re-acquisition, no hide-your-hands ritual. Verified on both headsets | modules/webxr | not yet |

| 51 | [shared] webxr: Omit the layer depth buffer on the per-view blit path | The blit path copies color only, so the projection layers depth buffer stayed zero-filled for the whole session - ~20-24 MB of GPU memory per stereo session wasted and spec-undefined input for any depth-using compositor reprojection. Paths that render directly into the layer (multiview, mono) keep their depth. Future quality item: blit real depth for depth-assisted reprojection | modules/webxr | not yet |

| 52 | [shared] webxr: Send the depthSensing init dict on every session | The depth-sensing feature is only granted when the usagePreference/dataFormatPreference dict accompanies it; the glue attached it only on WebGPU-bound sessions, so WebGL sessions requested the feature bare and browsers silently dropped it. Android XR grants depth to WebGL sessions (verified by hard probe on a Galaxy XR); usagePreference now also lists cpu-optimized. Unlocks the planned WebGL sensor-occlusion path | ~6 library_godot_webxr.js | not yet |

| 62 | webgpu: Lower dynamic binding-array indexing to per-element switches | fan_out_binding_arrays runs before the truncation fallback: handle arrays fan out to the driver's per-element layout (base 512 + binding*stride + element; stride widened 16->32 for lightmap_textures' MAX_LIGHTMAP_TEXTURES*2 elements), constant indices rewrite their consumers to the element variable directly (material-sampler class arrays now bind their TRUE elements instead of element 0), and dynamic indices lower to a structured OpSwitch cloning the consumer chain per element with the value merged by OpPhi - the lowering the sized-binding-arrays proposal endorses. Supported consumer shapes: load->image op, load->OpSampledImage->image op, and chain->helper call (the bicubic lightmap filter); anything else bails the variable to the truncation fallback (yesterday's behavior as worst case). Restores real multi-lightmap on web - the dwalter fork's documented "no multi-lightmap" limitation. Verified: bake exclusion parity with a pass-disabled control (only the two pre-existing subpass tonemap variants), fanned WGSL inspected (16 lightmap bindings + 12 switch sites), full 3D demo (shadows/sky/particles/glow) boots stock Chrome at 199fps with zero errors, strict probe pixel-exact. Runtime multi-lightmap pixels still need a lightmapped scene on-device; fragment texture budget for lightmap variants needs adapter maxSampledTexturesPerShaderStage > 16 | 0 | not yet |

| 63 | webgpu: Address array layers in texel copy regions | The texel-copy helper dropped the subresource's array layer, so every layer of a texture array uploaded onto layer 0 (last slice wins, all other layers empty) - lightmap texture arrays uploaded corrupted and baked scenes rendered black. WebGPU addresses layers through origin.z; RD supplies layers via the subresource with z = 0 for arrays, so origin.z += layer is unambiguous. Covers B2T, T2B, T2T, and the swizzle-expand path. Found by the first lightmapped scene ever run on the backend; with it, a fully-baked scene matches the native Vulkan ground truth on stock Chrome through patch 62's switch lowering | 0 | not yet |
| 64 | webgpu: Bake the Forward+ clustered shader family | Four SPIR-V passes plus a bake gate take the clustered census from 386 exclusions to 7 (all optional debug/fog/FSR2 variants): rewrite_copy_logical lowers the SPIR-V 1.4 OpCopyLogical to recursive per-member OpCompositeExtract/Construct (OpCopyObject demands identical types, so the naive swap fails validation; the scene shader copies its UBO SceneData into a logically-matching local struct ~200 times per bake); strip_nonwritable_on_function_vars drops glslang's 1.4-legal NonWritable hint on function-local const-array copies; lower_subgroup_ops_to_single_invocation rewrites the clustered light loops' subgroup operations to exact wave-of-1 semantics (reductions/broadcasts become the value, ballots a one-lane mask - the ops are a wave-coherence optimization over idempotent merges, so every-thread execution is correct); lower_helper_invocation_to_false turns the HelperInvocation builtin into a Private false constant (upstream's own sc_use_helper_check-off mode, shipped on Apple). The bake-side rw-storage exclusion is replaced by a WGSL-text gate matching WebGPU's real rule (write access allows the full storage format list; read/read_write stay r32-only), recovering ~70 write-only compute variants (glow, LUT generators), and the reserved-word rename covers the full WGSL-reserved/GLSL-legal identifier set (a clustered shader used 'from'). Mobile bake regression: exactly the two known subpass tonemap exclusions | 0 | not yet |
| 65 | webgpu: Support the clustered renderer at runtime | The baked WGSL becomes the authoritative source for bind group layout typing, closing the reflection-cannot-express class: storage texture <format, access> (reflection lacks the format for GLSL format-less writeonly images - marshalling Undefined aborted the JS bridge), sampled component types (Tint converts read-only storage images into sampled textures and types texture_2d<u32/i32>), storage buffer access per stage (reflection's stage-merged writable produced read-write layouts with Vertex visibility, which WebGPU forbids), and multisampled bindings (flag + UnfilterableFloat, incl. the combined-sampler split entries the MSAA resolve uses). Dead-eliminated bindings get self-consistent placeholders (r32float storage image + substitute texture; ReadOnly buffers; no fan-out past maxBindingsPerBindGroup) so partially-dead layouts can never poison a frame. command_resolve_texture is implemented as an empty render pass with a resolve target (Forward+ resolves MSAA by command, not subpass). Combined depth/stencil textures bound for sampling get lazily-created DepthOnly-aspect views. Storage textures always carry TextureBinding and CopyDst (the readonly-conversion and clear paths need them); clears of non-renderable or 3D textures (fog maps) zero-fill through writeTexture. The sRGB view sibling is only declared for non-storage textures, with sibling views of storage textures degrading to the owner format (the pinned Dawn ignores per-view usage; the old rule silently stripped storage from every rgba8 LUT, discarding their init dispatches - the source of the all-black lighting). Depth32FloatStencil8 reports unsupported (optional feature, not requested), sampler LOD clamps to WebGPU's valid range, SUPPORTS_FRAGMENT_SHADER_WITH_ONLY_SIDE_EFFECTS returns false (render pipelines need an attachment), and shaders carry their class name as the Dawn object label so validation messages identify the offender | 0 | not yet |
| 66 | [shared] rendering: Complete the cluster builder attachment fallback | The SHADER_USE_ATTACHMENT variant existed for drivers without SUPPORTS_FRAGMENT_SHADER_WITH_ONLY_SIDE_EFFECTS, but the path was never completed: setup() still created an empty framebuffer (no attachment for the variant's color output) and the MSAA pipeline was built against the samples-1 format. Every existing driver reports the feature, so the gap was dead code until WebGPU (whose render pipelines require an attachment) took the branch. The constructor now builds a SAMPLES_4 sibling format for the MSAA pipeline and setup() creates a matching dummy RGBA8 color target, freed with the rest of the state. No behavior change on drivers with the feature. Upstream-PR candidate | 34 | not yet |
| 67 | [shared] rendering: Fall back to supported LUT storage formats | The clustered renderer's pre-integrated LUTs assume r8unorm (best-fit normals) and rg16float (DFG) storage image support, which WebGPU and some mobile hardware lack. Both generators gain a fallback variant (rgba8 / rgba16f - only the original channels are ever sampled) picked by probing texture_is_format_supported_for_usage, with the texture format switched to match. integrate_dfg.glsl was also missing its #VERSION_DEFINES marker, so variant defines were silently never injected. Zero change on drivers with native support. These LUTs feed the PBR pipeline's energy terms; without them every light multiplies to black | 36 | not yet |
| 68 | [shared] rendering: Use raster octmaps whenever storage support is missing | The rgb10a2-storage probe guarding the compute octmap path only ran for the Mobile renderer (!can_use_storage); Forward+ assumed the format unconditionally, wedging sky and reflection radiance on drivers without it. The existing probe moves out of the mobile-only branch so any renderer redirects the octmap effects to their raster variants - the redirect upstream's own comment describes. Desktop drivers pass the probe and keep the compute path unchanged. Upstream-PR candidate | 18 | not yet |
| 69 | [shared] web: Request compute limits from the adapter | The device request copies five more adapter limits (maxComputeInvocationsPerWorkgroup and the workgroup size/storage limits): several clustered-renderer compute shaders exceed WebGPU's 256-invocation default, and creation fails at pipeline time with the default-limit device even though the adapter supports more | 5 | not yet |
| 70 | docs: Record the Forward+ state and future work | Coverage section for both renderers, the 7-variant exclusion census, and the future-work ladder: upstream the shared fallbacks, SSAO/SSIL format ports, realtime GI's image-atomics dependency (buffer-atomics port or the texture-atomics proposal), native WGSL subgroups, wider read-only storage textures, KTX2 compression, perf follow-ups, parked WebGPU-XR | 0 | pushed |
| 71 | webgpu: Substitute storage image formats WGSL cannot express | The screen-space effect family assumes r16f/r8/rg8-class storage images, none of which exist in WGSL. A bake-time SPIR-V pass substitutes each with a value-compatible legal format (r16f->r32f, r8->r32f - read_write legal where rgba8 would not be - rg8->rgba8, rg16f/r11g11b10->rgba16f, 16-bit ints widen to 32), then dedupes the type declarations the substitution merges (SPIR-V forbids duplicate non-aggregate types), remapping references at opcode-aware type positions. The driver mirrors the substitution when a storage-usage texture is created and reports storage support for substitutable formats, so the WGSL-declaration-driven layouts agree end to end. The bake gate also now allows read access for every storage format (Tint emits the shipped readonly_and_readwrite_storage_textures WGSL feature itself). Unlocks SSAO, SSIL, the last SSR variant, and CanvasSdf; remaining exclusions are exactly the image-atomics/int16 classes. Mobile regression zero errors | 0 | not yet |
| 72 | [shared] rendering: Use raster depth of field without wide read-write storage | The compute bokeh mutates the scene color through a read-write rgba16f storage image, which WebGPU restricts to 32-bit single-channel formats; a new SUPPORTS_READ_WRITE_STORAGE_IMAGES_ANY_FORMAT capability (true on Vulkan/D3D12/Metal, false on WebGPU) redirects those drivers to the raster bokeh, with the weight-buffer allocation extracted into RenderSceneBuffersRD::allocate_weight_buffers so storage-capable renderers can create it on demand, and the blur chain gaining the color-attachment usage the raster passes render into. Desktop drivers keep the compute path bit-for-bit. Depth of field verified on the Forward+ showcase (background bokeh, sharp focal plane) | 44 | not yet |
| 73 | webgpu: Bake the fog-volume fallback and widen rg16float | Upstream's volumetric fog already ships a buffer-atomics fallback for drivers without image atomics (SUPPORTS_IMAGE_ATOMIC_32_BIT, the MoltenVK capability this driver also reports false) - its variants just could not bake: the fog maps' Volatile and Coherent member decorations have no WGSL equivalent and Tint rejects them; the atomic operations provide the ordering the shader relies on, so a pass strips the hints. rg16float also substitutes globally rather than only for storage usage: the motion-vector buffers pair an rg16f MSAA attachment with a storage-substituted resolve target and resolve pairs must match formats (fragment outputs and sampling are format-agnostic in WGSL). FogVolume nodes and TAA both verified on the Forward+ showcase; mobile regression zero errors | 0 | not yet |
| 74 | webgpu: Widen the rg8ui voxel GI buffer for storage use | The clustered renderer's per-pixel voxel GI index buffer is rg8ui, created with storage usage whenever storage is available (the MSAA GI resolve writes it as a storage image), and rg8ui is not a WGSL storage format. rg8uint widens globally to rg32uint (attachment, storage, and sampled views must agree; the two-component uint fragment output and u32 sampling are unaffected), with single-sample scope enforced at texture and render-pass creation since rg32uint cannot be multisampled - the MSAA twin stays rg8uint and is consumed by the compute resolve, which samples it format-agnostically. The bake-side SPIR-V substitution gains the matching rg8ui->rg32ui row, un-excluding the voxel GI resolve variant. First half of the static-VoxelGI bring-up | 0 | not yet |
| 75 | webgpu: Bind depth textures in textureLoad-only float slots | The GI pass reads the raw scene depth with texelFetch through a plain texture2D, which Tint emits as textureLoad on texture_2d<f32> - no texture_depth_ declaration, so the driver's depth-in-float-slot rule bound the zero placeholder and every reconstructed vertex sat on the far plane (probe bounds-reject, black ambient buffer). Texture slots whose WGSL identifier only ever appears in its declaration or directly inside textureLoad are now typed UnfilterableFloat in the layout - legal for every float format and additionally for depth-aspect views - and the bind path then uses the real depth-only view instead of the placeholder. The load-only test is occurrence-based rather than call-site-based because Tint routes textures through helper-function parameters (the cone-trace helpers), which Dawn's static texture/sampler pairing validation follows. Completes the static-VoxelGI bring-up: the Cornell probe scene matches the Vulkan ground truth with MSAA 4x, strict probe pixel-exact, mobile and Forward+ Cornell regressions clean | 0 | not yet |
| 76 | docs: Record static VoxelGI and the paused GI roadmap | Coverage section refreshed to patch 75: Forward+ now lists SSAO/SSIL/SSR/TAA/DOF/FogVolumes and static VoxelGI as running; the exclusion census updated to the 45 blocked-class variants; the realtime-GI item rewritten with the corrected findings (the image-atomics claim was overbroad - upstream ships an unwired NO_IMAGE_ATOMICS fallback, and the GI sampling side already bakes clean) and the deliberate pause: the remaining rungs (dynamic-object VoxelGI, SDFGI voxelization wiring, SDFGI integrate buffer-ization) each require edits to actively-maintained upstream renderer files, mapped here with per-rung scope and risk for whenever work resumes | 0 | not yet |
| 77 | misc: Harden upstream-sync reporting against a missing label | The weekly upstream-sync rebase replayed all 77 patches cleanly across 197 new upstream commits but the run went red: its issue-reporting step called gh issue create --label upstream-sync when the label did not exist in the repo, and reporting failures fail the whole run (the previous no-op runs never reached that step). The workflow now creates the tracking label idempotently every run (gh label create --force) and both report steps are continue-on-error with guarded gh calls, so an informational reporting hiccup can never mask a clean rebase - a real rebase or verify failure still fails the run. Fork-local tooling; not upstreamed | 0 | not yet |

| 78 | misc: Give upstream-sync a token that may carry workflow updates | The weekly rebase replayed all 78 patches cleanly across 174 new upstream commits, then went red pushing the candidate: one of those commits (CI: Update all GitHub Actions to latest versions) edits seven files under .github/workflows/, and the token GitHub hands to a workflow run is categorically forbidden from creating or updating workflow files - no permissions: key grants it. The checkout now takes an optional SYNC_PAT repository secret (fine-grained token, Contents and Workflows read and write) and falls back to the default token where the secret is absent, so the push succeeds on weeks that carry upstream workflow changes. The run's verdict also moves to where the meaning is: a clean replay that cannot publish its candidate now warns and stays green (the question the workflow answers - do the patches still apply? - has been answered, and the replay reproduces locally byte for byte), with the tracking issue saying which of the two happened; a conflicted rebase or failed verification still fails. Also pins every gh call to this fork with GH_REPO: once the upstream remote exists, gh's remote ranking prefers a remote named "upstream", so the reporting steps had been querying godotengine/godot - the real reason for the could-not-create-issue warnings that patch 77 attributed to the missing label. Fork-local tooling; not upstreamed | 0 | not yet |

| 79 | misc: Add a local gate runner for stack promotion | CI answers whether the patches still apply and still compile; it cannot answer whether the engine still renders, because the shader bake needs tint, the pixel probe needs a real GPU and the boot check needs Chrome. That half was eight manual steps whose failure modes are all silent: an editor and a template built at different commits produce a bake the runtime cannot find, a headless export skips the baker entirely, a stale export customization cache reuses shaders from an older build, a missing target folder aborts the export at the end, and concurrent SCons runs corrupt a signature database shared across invocations. run-local-gates.sh performs the sequence in the only safe order, asserts the version-hash pairing before trusting any result downstream, reads the boot verdict from the console log (the harness always exits 0) with invalid command buffers as a hard failure, restores the flipped renderer config through a trap, and prints one pass/fail table. --quick halves build time by skipping the nothreads template; --forward-plus adds the clustered bake, the only way the Forward+ shader family is exercised. Fork-local tooling; not upstreamed | 0 | not yet |

| 80 | misc: Gate the shader bake in CI against a recorded census | The bake is where SPIR-V becomes WGSL, so it is the one place an upstream shader change surfaces as something WebGPU cannot express - and translation failures are deliberately non-fatal (the variant is excluded, the renderer takes another path), so nothing notices unless the exclusions are counted. Adds a fixture project whose scene is generated by the engine rather than hand-written (a hand-authored .tscn can carry property combinations the editor strips on save, and a stripped property changes the generated shader), a recorded census with each blocked class explained, a comparison script that fails on a new class or a grown count while treating shrinkage as an improvement to re-baseline, and CI steps hanging off the existing linux-editor job: tint comes from the release the tint-builder workflow publishes, lavapipe supplies a software Vulkan device, and xvfb satisfies the baker's requirement that the editor not be headless. Uses --export-pack, which runs the baker without needing a web template. The fixture reproduces the full project's census exactly - 8 exclusions, TonemapMobile x2 and Octmap x6. Fork-local tooling; not upstreamed | 0 | not yet |

| 84 | misc: Gate the Forward+ bake census in CI | The census gate covered the Mobile renderer only, leaving the clustered family - the larger and more exclusion-prone of the two - unguarded: an upstream shader change could break a Forward+ variant and only surface at the next local promotion. The CI leg now bakes the same fixture a second time with the renderer flipped to forward_plus and compares against a second recorded baseline (37 exclusions, measured; fewer than the full test project because variant instantiation is partly content-driven, but every blocked class is represented). Fork-local tooling; not upstreamed | 0 | not yet |
| 85 | [shared] webxr: Expose fixed foveation on projection layers | Fixed foveation - rendering the periphery at reduced resolution where the eye cannot see detail - is the single most effective performance lever on standalone headsets, and the WebXR Layers API exposes it as a mutable fixedFoveation attribute (0..1) that the browser compositor applies at zero engine cost. Godot had no API for it on any WebXR path. Adds WebXRInterface.fixed_foveation, plumbed exactly like display_refresh_rate: the glue clamps and stores the request, applies it when a projection layer is created (GL and WebGPU binding paths both) and live mid-session, and the getter reads the layer attribute back because the runtime may clamp the request. The support check is per-instance ("fixedFoveation" in layer), not prototype-based: Quest Browser 149 exposes the attribute on the created XRProjectionLayer while its XRCompositionLayer prototype lacks it. Verified on-device (set 1.0 reads back 1.0, periphery softens). Unsupporting browsers see no behavior change. Strong upstream candidate: mirrors an existing WebXRInterface property pattern, no WebGPU dependency | ~65 across 8 modules/webxr files (all SHARED_OK) | not yet |
| 86 | [shared] webgpu: Adopt FP16 shaders on devices with shader-f16 | Mobile GPUs prefer half floats - half the bandwidth, double-rate math, less heat - and upstream ships FP16 variant groups of its scene shaders selected at runtime via SUPPORTS_HALF_FLOAT. The web pipeline previously opted out at every layer: the baker skipped FP16 groups, the loader never requested the shader-f16 device feature, and the driver hard-coded the capability false. Now the loader requests shader-f16 when the adapter offers it (Quest Browser and desktop Chrome both do), the driver reports SUPPORTS_HALF_FLOAT from the actual device feature, the module-rejection guard for `enable f16` only fires when the feature is genuinely absent, and the baker bakes both FP32 and FP16 groups so the runtime can select either - native-parity behavior. Measured: 100 of 430 baked WGSL modules in the census fixture carry `enable f16;`, both CI census baselines pass unchanged (no new exclusion classes - tint translates every FP16 variant), and the xrtest WebGPU export boots desktop Chrome on the FP16 group with zero errors. Cost: the exported shader cache grows ~33% (xrtest pck 10.4 -> 13.8 MB); if that matters for a project, a future export option can restore FP32-only baking | loader ~5, driver ~15, baker ~-8 | not yet |
| 88 | webgpu: Make FP16 shader baking an export choice | Baking both float widths costs ~3.5 MB of already-compressed shader cache per export (the baked WGSL is zstd-compressed, so it does not gzip away), paid by every visitor including ones whose device never selects the FP16 group. The baker now honors a project setting, rendering/webgpu/bake_fp16_shader_variants (default true - the perf-first native-parity behavior), and the godot_webgpu addon surfaces it as a "Bake FP16 Variants" checkbox on the export preset, applied transiently at export begin like the addon's other renderer settings. Off restores the exact pre-FP16 bake: verified 0 f16 modules and the original pck size through both the engine setting and the addon option | 0 | not yet |
| 89 | [shared] web: Request a high-performance WebGPU adapter | requestAdapter() was called with no powerPreference, leaving the choice to the browser default - which on a hybrid-graphics laptop is the integrated GPU. Measured on a laptop with a discrete NVIDIA part: the stress scene ran at 10 fps on the Intel UHD the browser had picked, and 155 fps once the discrete GPU was used. Every 3D WebGPU export was silently paying this. Requests high-performance; the member is ignored where unsupported and composes with the existing xrCompatible request | engine.js ~6 | not yet |
| 90 | [shared] webgpu: Sample volumetric fog through an unshared sampler | Godot reuses one linear sampler for both the shadow atlases and other textures. WebGPU permits only nearest filtering on depth formats, so a sampler statically paired with a depth texture must be declared non-filtering - and that verdict follows the sampler, silently point-sampling every other texture it serves. Two were affected: the scene shader applies the froxel volume through SAMPLER_LINEAR_CLAMP (visible as stepped fog against geometry from any angle), and the fog process shader reprojects the previous frame through linear_sampler (which stopped temporal accumulation from averaging its own per-frame jitter, visible as vibration at fog/geometry boundaries). Both now use the equivalent linear-with-mipmaps sampler, already bound in the same set with identical filter and clamp settings but no depth pairing. Measured on the stress scene: gradient step 2.90 -> 1.43 against a native Vulkan reference of 1.39; no measurable frame cost. Behaviour is unchanged on backends that can filter depth textures | 2 shader lines | not yet |
| 91 | webgpu: Read the decal atlas through its sRGB view | A view may only use a format the texture declared in viewFormats, and an sRGB view may not carry storage usage. WebGPU lets a view name its own usage to resolve that, but the pinned emdawnwebgpu bindings do not marshal WGPUTextureViewDescriptor::usage (they marshal label/format/dimension/mip/layer/aspect and nothing else), so a view always inherits the texture's - meaning a texture cannot be both storage-capable and readable as sRGB. Storage-capable textures were therefore skipped entirely, which silently cost the decal atlas its sRGB decode: it is storage-capable and is read through an sRGB view, so decals sampled without the sRGB->linear conversion, roughly doubling their mid-tones until they saturated into a hard-edged block (measured as the decal contribution's G/R ratio: 0.691 against 0.318 for a correct decode, 0.600 for none; 0.335 after). The engine names its intent in shareable_formats, so a texture that lists the sibling gets it declared and gives up its storage bit instead - the decal atlas is filled by raster draws and never bound as a storage image. The duplicated sRGB view-usage block in texture_create_shared is folded into one helper and applied to the slice path too | 0 | not yet |
| 92 | [shared] webgpu: Honor depth clamping where the browser offers it | PipelineRasterizationState::enable_depth_clamp was dropped on the floor: unclippedDepth was never set and depth-clip-control never requested, so geometry crossing the near plane was clipped instead of clamped. The clustered builder rasterizes light, decal and probe proxy volumes and asks for clamping precisely because a camera sitting inside a proxy would otherwise lose the part in front of the near plane. The loader requests the feature adapter-gated, next to the existing float32-filterable and shader-f16 requests, and the driver applies it when granted. Allowlist unchanged (engine.js already covered) | 6 | not yet |
| 93 | [shared] rendering: Extract the cluster box scale from the basis columns | ClusterBuilderRD::add_box folded a decal's or reflection probe's node scale into its half-extents using the length of each basis ROW. The per-axis scale of a basis is the length of its COLUMNS; rows only agree when no rotation is present, and xform is view_xform * p_transform, so a rotation is always present. Anything scaled through its node transform rather than through its size property was therefore clipped to a proxy box built from a mixture of its axes - one that changed shape as the camera turned, so it read as jagged edges while orbiting rather than as a wrong size. Minimal repro (floor, one light, one opaque decal, one camera): the same 11x4x11 box authored via size spans the authored rows 83..438, authored via a basis scale it spanned 128..352; after the fix both span 83..438. Not WebGPU-specific - it reproduces identically on native Vulkan. Upstream-PR candidate | 18 | not yet |
| 94 | [shared] webgpu: Implement timestamp queries for GPU profiling | viewport_get_measured_render_time_gpu read 0 on the web, so the backend could not be profiled at all and every perf number had to be taken from native Vulkan as a stand-in - a different driver, which by construction cannot show WebGPU-specific cost. WebGPU has no encoder-level writeTimestamp: timestamps may only be written at pass boundaries. The engine always captures BETWEEN passes (capture_timestamp refuses to run inside an open list), so each capture rides its own empty compute pass, which costs nothing and lands exactly where it was asked for. Two traps: the pinned emdawnwebgpu bindings marshal BOTH pass write indices unconditionally, so WGPU_QUERY_SET_INDEX_UNDEFINED reaches JS as a literal 0xFFFFFFFF and invalidates the pass - the port cannot express 'no end-of-pass timestamp', so the unwanted write points at a scratch slot; and resolving per command-buffer-end captured queries that had not been written yet (the engine submits several per frame), so the resolve is one dedicated encoder submitted after the frame. Readback is async (no synchronous map on the web), so results are served from the last completed map - profiling numbers a frame or two old. Without the feature the pool stays a benign token and reads zeros, exactly as before. Measured working on STOCK Chrome, no developer flags: steady 0.87-0.92 ms on the stress scene. Chrome quantizes timestamps to ~100us, which is far below a frame's cost but does hide sub-0.1ms effects | 7 | not yet |
| 104 | [shared] webxr: Move stack-added exports into a sibling JS library | library_godot_webxr.js carried 649 inserted lines against upstream, and most were whole functions upstream has no counterpart for (offer_session, get_depth_sensing_info, get_pass_info, uses_multiview, get_gl_pass_info, get_color_format, get_frame_matrices, set/get_fixed_foveation). Those nine exports move verbatim to library_godot_webxr_ext.js - a file that can never conflict on a rebase - shrinking the shared file to the changes genuinely interleaved with upstream code (425 inserted lines, -35%). autoAddDeps ties the sibling to $GodotWebXR, which stays where it is declared; when a feature is upstreamed, its export moves into the upstream file as part of that PR. Rows 95-103 pending backfill | 6 shared (SCsub registration) | not yet |
| 105 | [shared] webxr: Materialize hand joints before both fill calls | Patch 102's joint-overflow guard left hand.values() as a raw iterator on the everyday 25-joint path; fillPoses consumes an iterator, so fillJointRadii received an exhausted one (zero spaces against a 25-length buffer) and threw uncaught every frame, killing the frame callback - hands dead on every headset. The guard's own trim path (size > 25) already materialized an array and was fine. Array.from materializes the joint list once; both calls share it. Caught on the first on-device run of an export whose template actually carried patch 102's glue: headless drives inject at the tracker level, above this code - only a device executes it | modules/webxr glue | not yet |
| 106 | misc: Track upstream's Emscripten pin at 6.0.1 | Upstream 07be503875 raised the minimum Emscripten from 4.0.0 to 6.0.1 and moved web_builds.yml to that pin; the baseline workflow still installed 4.0.11, so both web-template legs on the first candidate carrying that commit failed the SCons sanity check in under a minute (exit 255) before compiling anything. The weekly sync's pin-drift probe named exactly this in the tracking issue, as designed. The bump also moves the emdawnwebgpu port from Dawn v20250531.224602 to v20260423.175430 - the rd leg now compiles the driver against eleven months newer webgpu.h - and, like any toolchain change, invalidates all previously recorded WASM size deltas (see the SIZE CONTRACT note in the workflow). Follow-ups tracked outside this patch: refresh the released tint to the same Dawn ref via tint-builder + the compare_wgsl.py equivalence gate, and install emsdk 6.0.1 locally before the next local gate run | 0 | not yet |
| 107 | webgpu: Strip aliasing hint decorations before Tint | glslang decorates every `restrict` buffer member and image with Restrict; Tint's original spirv-reader silently ignored the pair, but the rewritten IR reader shipped with Dawn v20260423 faults on member-level Restrict (TINT_UNIMPLEMENTED unhandled member decoration: 19) - 277 of the mobile renderer's variants at census, the whole scene-shader family included. Restrict and Aliased join Volatile and Coherent in the existing hint-strip pass (renamed strip_hint_decorations): WGSL has no aliasing annotations at all, so both are pure hints with nothing to lose. Proven a no-op under the production tint - bakes with and without the pass produce byte-identical WGSL sets (491/491) - and measured to clear the 277 under the new tint. The new tint still refuses ~76 variants for other reader regressions (failure map in the testbed's tint-adoption-v20260423/), which is why the pin in patch 108 stays | 0 | not yet |
| 108 | misc: Pin the CI bake tint by release tag | The bake leg downloaded the LATEST tint-builder release; dispatching tint-builder for Dawn v20260423 (per the emsdk-drift policy) silently moved that pointer to a tint whose stricter IR reader still refuses scene, particles-copy, skeleton and resolve variants - the census gate would have gone red on the next push, and the only green run at the new base beat the release by six minutes. The download now names tint-v20250531.224602 explicitly; the comment carries the adoption blockers and the rule that the pin moves only together with the local GODOT_TINT_PATH as one deliberate adoption | 0 | not yet |
| 109 | misc: Tolerate the Trail3D/Line3D missing-bake signature in the boot gate | Upstream f2aee4e142 added Trail3D and Line3D, whose spatial shaders are built from code strings at engine startup through shader_create_from_code - a path marked non-embedded, so the versions are invisible to all three bake enumeration paths and never reach the pck. On the web the runtime cannot compile GLSL, so every boot at this base prints four missing-version errors and the nodes render nothing; on Metal/D3D12 the same gap silently degrades to runtime compiles, which makes this an upstream shader-baker bug worth its own PR. The boot leg tolerates exactly this signature with the removal condition documented in place; the limitation ledger carries the row. Found by the promotion gates doing their job: the candidate's first boot leg went red on it | 0 | not yet |
| 110 | [shared] editor: Bake Trail3D's engine-static shaders explicitly | The real fix for the gap patch 109 tolerated: Trail3D exposes its four startup-built spatial shaders through a static getter, and the baker customizes their live versions right after the embedded-set loops - the same explicit-instantiation shape as the raster octmap block. Verified: a fresh export carries the two previously-missing MODE_UNSHADED versions, both renderers boot with zero missing-version errors, and the gate's tolerance signature is removed in the same change. The general upstream mechanism (an embedded channel for engine-static scene shaders) remains upstream-PR material - any future node of this class would regress the same way until it lands | 2 shared (trail_3d.h/.cpp getters; baker already allowlisted) | not yet |
| 111 | [shared] webgpu: Serve texture swizzles with native view swizzles | texture-component-swizzle has been stable since Chrome 143 and the Dawn v20260423 port marshals the chained descriptor, so the driver now creates the default view with a real component swizzle when the adapter grants the feature (loader requests it adapter-gated) - font atlases and other luminance/alpha layouts keep their native R8/RG8 formats instead of being promoted to RGBA8 and expanded texel-by-texel at upload. Swizzled views are sampling-only in WebGPU, so any attachment/storage usage (and any device without the feature, headless SwiftShader included) falls back to the existing CPU expansion unchanged. Verified: stress scene boots with 'texture swizzles served by native view swizzles', zero errors, crisp text; first of the 2026-08 landscape integrations (transient attachments and native subgroups queued behind it) | ~8 (engine.js feature request) | not yet |
| 112 | webgpu: Honor the transient-attachment bit with tile-memory scratch | Chromium 149 added GPUTextureUsage.TRANSIENT_ATTACHMENT: a render attachment whose contents never leave the pass stays in on-chip tile memory without allocating VRAM - the biggest bandwidth lever on mobile GPUs (Quest included). The engine already models exactly that contract as TEXTURE_USAGE_TRANSIENT_BIT (the Vulkan driver maps it to lazily-allocated transient images), so the driver now maps the bit to the WebGPU flag, collapsing usage to the attachment alone and skipping the sRGB sibling declaration per the transient rules; browsers without the flag (probed via the interface constant, no adapter feature exists) treat the texture as a plain attachment. Inert today: upstream sets the bit nowhere yet (render_scene_buffers_rd.cpp:188 carries the TODO 'detect when it is safe' for RB_TEX_COLOR_MSAA and depth) - marking the mobile renderer's MSAA scratch transient is the follow-up arc, shared-file and native-behavior-affecting, and a natural upstream PR resolving their own TODO | 0 | not yet |

(Rows 53-61 pending backfill. Planned next: perf/pipeline warm-up passes for XR; Quest Browser ships
experimental WebXR-WebGPU since April 2026. NOTE: upstream
already compiles RenderingDevice + renderer_rd unconditionally on all
platforms including web; the per-platform RD_ENABLED define is the only
gate, which is why patch 01 is a detect.py-only change.)

## Renderer coverage and future work (as of patch 75)

### What runs today

Both RenderingDevice renderers render on WebGPU in stock Chromium
browsers with no flags, from the same AOT-baked WGSL pipeline:

- **Forward Mobile** (the shipping default): full 2D/3D, shadows, sky,
  GPU particles, glow, MSAA 4x, baked lightmaps with dynamic-index
  binding arrays, light probes, reflection probes (UPDATE_ONCE), mixed
  lighting, WebXR (per-view path; Quest and Galaxy XR verified).
- **Forward+ (clustered)** (patches 62-75): clustered
  omni/spot/decal/probe lighting, cascaded directional shadows, MSAA
  command resolve, environmental volumetric fog, FogVolume nodes,
  reflection probes in both UPDATE_ONCE and UPDATE_ALWAYS (realtime)
  modes, baked lightmaps, the DFG/best-fit-normal PBR pipeline, SSAO,
  SSIL, SSR, TAA, depth of field (raster path), and **static VoxelGI**
  (editor-baked voxel cone tracing with runtime light injection: moving
  lights drag their bounced light live, emissive surfaces illuminate
  with no light nodes; verified against the Vulkan render of the same
  scene, with and without MSAA 4x). Verified against the Mobile render
  of the same scene.

Firefox (wgpu/naga) validates the same baked WGSL for boot, 2D, 3D,
and compute.

### Limitation ledger

One row per thing that does not work, what it looks like from the
outside, and what would lift it. "Whose fix" is the important column:
most of what is visible in a console today is ours to remove and is not
a capability limit at all.

| Symptom | Feature affected | Root cause | Whose fix | Lifts when |
| --- | --- | --- | --- | --- |
| `Parameter "shader" is null` (compute_pipeline_create, uniform_set_create, pipeline_cache_rd) | none - cosmetic | a variant was excluded at bake and the effect builds pipelines from the null shader anyway | **ours** | the setup paths check the shader, or a capability stops those systems initialising. Pure noise; nothing behaves differently |
| Dawn: read-write storage buffer with `ShaderStage::Vertex` (`VoxelGiDebugShaderRD`) | VoxelGI debug view | WebGPU forbids writable storage buffers in vertex stages | WebGPU spec, or a port | the debug shader moves its write to a fragment/compute stage. Debug visualisation only |
| Dawn: 10 storage textures in the compute stage exceeds the limit of 8 (`SdfgiPreprocessShaderRD`) | SDFGI preprocess | the shader binds more storage textures than this adapter allows per stage | upstream, or hardware | the pass is split across dispatches, or an adapter reports a higher `maxStorageTexturesPerShaderStage`. Reported as a format error until patch 83 fixed the format mismatch that masked it |
| bake: `SceneForwardClustered` x16 | SDFGI voxelization | image atomics; upstream ships a NO_IMAGE_ATOMICS fallback that only fog.cpp ever enables | **ours + upstream** | the existing fallback variant group is wired in scene_shader_forward_clustered.cpp |
| bake: `VoxelGi` x3 (MODE_DYNAMIC) | dynamic-object VoxelGI relighting | rgba16f read-write storage images; WGSL allows read-write on r32 formats only | **ours** | split into a read+write pair, or pack through r32ui |
| bake: `SdfgiIntegrate` x1 | SDFGI probe integration | rgba16i/rgba32i read-write accumulators | **ours** | the accumulators move to storage buffers |
| bake: `FsrUpscale` / FSR2 | FSR2 upscaling | 16-bit integer math | browser + loader | the device exposes shader-f16 and the loader requests it |
| bake: `Octmap*` x6 | compute octmap prefilter | rgb10a2 is not a WGSL storage format | already routed around | patch 68 takes the raster octmap path instead; nothing is missing at runtime |
| bake: `TonemapMobile` x2 | subpass tonemapping | WebGPU has no subpass inputs at all | WebGPU spec | architectural - not expected to change. The non-subpass path is used and is equivalent |
| boot: `SceneForward*ShaderRD is missing from the baked shader cache` x4 at startup | Trail3D / Line3D render nothing on web | the nodes (upstream f2aee4e142) build spatial shaders from code strings at engine startup via shader_create_from_code, which is non-embedded - invisible to every bake enumeration path, so their versions are never baked and the web runtime cannot compile them | **upstream** (affects every shader-baker platform; Metal/D3D12 silently fall back to runtime compiles) | LIFTED IN-STACK by patch 110: the baker bakes Trail3D's shaders explicitly (octmap-block pattern) and the gate tolerance is removed. The GENERAL upstream mechanism (a channel for engine-static scene shaders) is still worth an upstream PR - any future node of this class regresses the same way |
| no VRS | variable rate shading | WebGPU has no concept of it | WebGPU spec | a future spec addition |

The bake census gate in CI enforces the middle of this table
mechanically: a new exclusion class, or a growing count in an existing
one, fails the build rather than quietly widening the list.

### Known exclusions (bake census: 45 variants)

All remaining exclusions are the image-atomics / int16 / read-write
storage classes: VoxelGI dynamic-object relighting (3) and its debug
visualization (3), SDFGI voxelization (clustered scene variants) and
the probe-integration accumulator (1), FSR2 (int16), and legacy
multiview/variant-0 holes. Every excluded shader fails cleanly to a
null variant the engine tolerates; nothing in the supported feature set
depends on them.

### Future opportunities, in rough order of value

1. **Upstream the shared fallbacks.** Patches 66 (cluster builder
   attachment completion), 67 (LUT storage format fallbacks), and 68
   (octmap raster redirect) are capability fixes any limited driver
   needs, written without WebGPU references. Each merged PR leaves this
   stack and takes its rebase surface with it.
2. **Upstream the DOF capability (patch 72).** The
   SUPPORTS_READ_WRITE_STORAGE_IMAGES_ANY_FORMAT capability plus the
   completed raster bokeh path is the strongest standalone PR in the
   stack: a real capability, a completed fallback, no WebGPU
   references. (SSAO/SSIL/SSR/CanvasSdf shipped in patch 71 via the
   storage-format substitution; no engine changes were needed.)
3. **Remaining realtime GI (paused here by design).** Static VoxelGI
   works today (patches 74-75); the rest was investigated and scoped,
   then deliberately paused because every remaining piece must edit
   actively-maintained upstream renderer files - unlike patches 74-75,
   which touched only this driver. The map, in ascending risk:
   - *Dynamic-object VoxelGI* (3 variants): moving objects plotted into
     the voxel field. Blocked on rgba16f/rgba8 read-write storage
     images (WGSL allows read-write on r32 formats only); port = split
     each into a read+write pair or an r32ui packing under a
     driver-gated variant. Touches voxel_gi.glsl and the gi.cpp uniform
     sets. Moderate, independently shippable.
   - *SDFGI voxelization*: upstream already ships a NO_IMAGE_ATOMICS
     GLSL fallback in the clustered scene shader (plain load-or-store
     on r32ui, WebGPU-legal) but only the fog system ever defines the
     macro - the scene shader's fallback is dead code. Wiring it is a
     variant-group addition in scene_shader_forward_clustered.cpp
     mirroring fog.cpp's use of SUPPORTS_IMAGE_ATOMIC_32_BIT. Low risk,
     and the strongest upstream-PR framing of anything here
     ("complete coverage of your own capability fallback").
   - *SDFGI probe integration*: the genuinely structural piece. The
     probe history/average accumulators are rgba16i/rgba32i read-write
     images (load-modify-store on the same texture); the port converts
     them to storage buffers under the same capability, with
     allocation plumbing in gi.cpp (~4,000 actively-maintained lines -
     the largest rebase-conflict surface of the remaining work). SDFGI
     needs both of its pieces before anything renders.
   The GI *sampling* side (GiShaderRD) already bakes with zero
   exclusions for both systems, so each port lights up consumption
   that is already proven. Mitigation when resumed: capability-gated
   fallbacks with no #ifdefs, byte-identical on capable drivers, and
   early upstream PRs so the patches exit this stack. Texture atomics
   remain a WebGPU proposal; if browsers ship them first, the
   exclusions clear without any of this.
4. **Native WGSL subgroups.** The wave-of-1 lowering (patch 64) is
   semantically exact but forfeits the wave-coherence optimization.
   WGSL now has a `subgroups` feature; when the pinned Tint's reader
   translates GroupNonUniform ops, drop the pass and request the device
   feature (adapter-gated, the loader pattern already exists).
5. **Read-only storage textures beyond r32.** The WGSL
   `readonly_and_readwrite_storage_textures` language feature widens
   the read/read_write format rules; adopting it relaxes the patch-64
   bake gate and removes Tint's sampled-texture conversion for
   read-only images.
6. **Texture compression.** Exports currently ship uncompressed RGBA8
   (VRAM compression off); lightmap EXRs need a manual import flip.
   Proper fix: KTX2/Basis transcode plus an adapter-gated
   texture-compression-bc feature request in the loader.
7. **Perf follow-ups.** The sky re-filters all radiance layers every
   frame on clustered (upstream behavior worth a dirty-check); render
   bundles (snapshot-style recording) remain the one technique peers
   ship that this backend lacks; MSAA 2x/8x still need a clamp to 4.
8. **WebGPU-XR (parked).** The stereo per-view path works end to end on
   Quest; re-entry is gated on browsers shedding the XRGPUBinding
   frame-copy tax and the second-session resume bug (see the parked
   notes in the project memory).

## Rules

- **One patch, one purpose.** Subject describes the whole diff with no "and".
- **Placement:** new WebGPU code only under `drivers/webgpu/`; new web glue
  as NEW files under `platform/web/`. New files are rebase-cheap; edits to
  existing files are a weekly tax.
- **Shared files allowed** (<= ~20 lines per patch per file, only when the
  patch's purpose requires it,
  commit tagged `[shared]` in the subject): `platform/web/detect.py`,
  `platform/web/SCsub`, `drivers/SCsub`, `main/main.cpp`,
  `servers/rendering_server.cpp`, `platform/web/js/engine/*`,
  `platform/web/display_server_web.cpp/.h` (added in patch 04: driver
  registration has no other home), `editor/editor_node.cpp`,
  `editor/shader/shader_baker/SCsub`,
  `platform/web/export/export_plugin.cpp/.h` (added in patch 07: shader
  baker registration and the web export option), and
  `editor/export/shader_baker_export_plugin.cpp/.h` (cpp added in patch 15;
  h added in patch 31 for the variant-veto hook),
  `modules/webxr/*` (added in patch 34: the WebXR implementation itself),
  `servers/xr/xr_interface.h` and
  `servers/rendering/renderer_viewport.cpp` (added in patch 35: the
  per-view draw-pass hook and its single call site).
- **Never touched:** `servers/rendering/rendering_device.cpp`,
  `rendering_device_graph.*`, `shader_compiler*`, `modules/glslang`,
  existing drivers, renderer_rd scene/effects code outside the designated
  fallback patch.
- **Size:** target <= 500 changed lines; ~1500 ceiling for pure new-file
  scaffolds only.
- **Clean-stack admission:** no placeholder shaders, no debug prints, no
  commented-out code, nothing that changes flag-off behavior, nothing you
  couldn't defend line-by-line in upstream review. Everything else lives
  in `webgpu-dev`.
- Enforced mechanically by `misc/webgpu_scripts/check-stack.sh`.
